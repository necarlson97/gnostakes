[project]
name = "gnostakes"
version = "0.0.0"
requires-python = ">=3.12"

[tool.setuptools]
package-dir = {"" = "src"}

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
